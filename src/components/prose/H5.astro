---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h5">;
const { class: className, ...rest } = Astro.props;
---

<h5 class:list={["mt-7 mb-2 scroll-mt-24 font-semibold leading-[1.4]", className]} {...rest}>
  <slot />
</h5>
