---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"ol">;
const { class: className, ...rest } = Astro.props;
---

<ol class:list={["my-4 list-decimal ps-6", className]} {...rest}>
  <slot />
</ol>
