title:
  en-US: web
  zh-Hans: Web
sidebarIcons:
  guide: tabler:route
  advanced: tabler:settings-code
  examples: tabler:flask-2
abbr:
  zh-Hans:
    HTTP: "Hypertext Transfer Protocol，超文本传输协议。"
    HTTPS: "通过 TLS 加密传输的 HTTP。"
    TLS: "Transport Layer Security，传输层安全协议。"
    URL: "Uniform Resource Locator，用于定位网络资源的地址。"
    handler: "接收 HTTP 请求并向 ResponseWriter 写入响应的处理器。"
    Handler: "接收 HTTP 请求并向 ResponseWriter 写入响应的处理器。"
    middleware: "包装下游 handler，在请求前后附加行为的中间件。"
    Middleware: "包装下游 handler，在请求前后附加行为的中间件。"
    router: "根据请求方法与路径选择 handler 的路由器。"
    Router: "根据请求方法与路径选择 handler 的路由器。"
    context: "随请求传播取消信号、截止时间和请求级数据的 context.Context。"
    header: "HTTP 头字段，用于传递请求或响应的元数据。"
    Header: "HTTP 头字段，用于传递请求或响应的元数据。"
    body: "HTTP 请求或响应承载的消息正文。"
    callback: "由类库在特定时机回调的应用函数。"
    streaming: "数据生成后持续分段写出，而不是先完整缓冲的传输方式。"
    WebSocket: "在单个连接上提供全双工通信的 WebSocket 协议。"
    SSE: "Server-Sent Events，服务器通过长连接向浏览器单向推送事件。"
    panic: "Go 中断当前调用流程并沿调用栈展开的运行时机制。"
    goroutine: "由 Go 运行时调度的轻量并发执行单元。"
    nil: "Go 中指针、接口、切片、映射等类型的零值。"
    route: "由请求匹配条件和目标 handler 组成的一条路由。"
    routes: "由请求匹配条件和目标 handler 组成的路由集合。"
    pattern: "描述请求方法、主机或路径如何匹配的模式。"
    patterns: "描述请求方法、主机或路径如何匹配的模式集合。"
    listener: "监听网络地址并接收连接的 net.Listener。"
    writer: "向响应或其他目标持续写入数据的对象。"
    deadline: "操作必须完成或被取消的绝对时间点。"
    timeout: "允许操作持续的最长时间。"
    redirect: "通过 3xx 响应指示客户端改用另一个 URL。"
    Cookie: "由服务端设置并由浏览器随后续请求携带的小型状态数据。"
    URI: "Uniform Resource Identifier，用于标识资源的字符串。"
    Referer: "HTTP 中记录来源页面 URL 的历史拼写头字段。"
    "User-Agent": "标识发起 HTTP 请求的软件及版本的请求头。"
    "Content-Type": "声明 HTTP 消息正文媒体类型的头字段。"
    "Content-Length": "声明 HTTP 消息正文预期字节数的头字段。"
    "Content-Encoding": "声明 HTTP 消息正文采用何种内容编码的头字段。"
    GET: "读取资源的 HTTP 方法，语义上应安全且幂等。"
    HEAD: "仅获取与 GET 相同响应头、不返回响应正文的 HTTP 方法。"
    POST: "向资源提交数据或触发处理的 HTTP 方法。"
    PUT: "以给定表示创建或整体替换资源的幂等 HTTP 方法。"
    PATCH: "对资源执行部分修改的 HTTP 方法。"
    DELETE: "请求删除资源的幂等 HTTP 方法。"
    OPTIONS: "查询目标资源通信选项的 HTTP 方法，也用于 CORS 预检。"
    metadata: "描述请求、响应或运行状态、但不是主要业务正文的数据。"
    tracing: "记录一次请求跨操作边界传播过程的追踪机制。"
    metrics: "按时间聚合的计数、延迟或容量等数值型观测数据。"
    recovery: "从 panic 或运行期失败中恢复到受控边界的过程。"
    cleanup: "停止工作后释放连接、文件和 goroutine 等资源的清理过程。"
    flush: "把缓冲数据立即推送到底层 writer 或客户端的操作。"
